<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chuizhipu.shop.mapper.ProductSkuMapper">

    <select id="selectByProductId" resultType="com.chuizhipu.shop.entity.ProductSku">
        SELECT id, product_id AS productId, name, price, stock, image
        FROM t_product_sku
        WHERE product_id = #{productId}
    </select>

    <select id="selectById" resultType="com.chuizhipu.shop.entity.ProductSku">
        SELECT id, product_id AS productId, name, price, stock, image
        FROM t_product_sku
        WHERE id = #{skuId}
    </select>

    <select id="selectByProductIds" resultType="com.chuizhipu.shop.entity.ProductSku">
        SELECT id, product_id AS productId, name, price, stock, image
        FROM t_product_sku
        WHERE product_id IN
        <foreach collection="productIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
    </select>

    <insert id="insertBatch">
        INSERT INTO t_product_sku (product_id, name, price, stock, image) VALUES
        <foreach collection="skus" item="sku" separator=",">
            (#{sku.productId}, #{sku.name}, #{sku.price}, #{sku.stock}, #{sku.image})
        </foreach>
    </insert>

</mapper>
