<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chuizhipu.shop.mapper.CommentMapper">

    <resultMap id="commentMap" type="com.chuizhipu.shop.entity.Comment">
        <id property="id" column="id"/>
        <result property="productId" column="product_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
        <result property="userName" column="user_name"/>
        <result property="avatar" column="avatar"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_comment (product_id, user_id, content)
        VALUES (#{productId}, #{userId}, #{content})
    </insert>

    <select id="selectByProductId" resultMap="commentMap">
        SELECT c.id, c.product_id, c.user_id, c.content, c.create_time,
               u.nickname AS user_name, u.avatar AS avatar
        FROM t_comment c
        LEFT JOIN t_user u ON c.user_id = u.id
        WHERE c.product_id = #{productId}
        ORDER BY c.create_time DESC
    </select>

</mapper>
